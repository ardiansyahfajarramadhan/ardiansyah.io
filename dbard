<?php
$servername = "localhost";
$username = "ardiansyah"; // Ganti dengan username Anda
$password = "22102004"; // Ganti dengan password Anda
$dbname = "db_ard"; // Ganti dengan nama database Anda

// Membuat koneksi
$conn = new mysqli($servername, $ardiansyah, $ardiansyah, $db_ard);

// Memeriksa koneksi
if ($conn->connect_error) {
    die("Koneksi gagal: " . $conn->connect_error);
}
echo "Koneksi berhasil!";
?>

